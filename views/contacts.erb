<!DOCTYPE html>
<html lang="en">
	<head>
	<meta charset="utf-8">
	<title>Contacts</title>
	</head>
	<body>
		<h1>All Contacts</h1>
		<ul>
		<% @contacts.each do |contact| %>
			<li><a href="/contacts/<%= contact.id %>"> <%= contact.first_name %> <%= contact.last_name%>, email: <%= contact.email%>, date of birth: <%= contact.date%>, notes: <%= contact.notes%></a></li>
		<% end %>
		</ul>
	</body>
</html>